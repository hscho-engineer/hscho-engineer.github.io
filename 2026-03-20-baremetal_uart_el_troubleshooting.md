# [OS 딥다이브] ARMv8 인터럽트 데드락 추적기: 부트로더의 가상화(EL2) 기만극과 의도적 추락(Drop to EL1)
## 1. 아키텍처의 한계 돌파: 무한 폴링(Polling)의 종말
C++ 비동기 서버의 뼈대를 세우기 위해, 기존 베어메탈 커널에 존재하던 끔찍한 UART 수신 대기열(while 무한 루프)을 도려내기로 결정했다. 비명을 지르는 하드웨어를 억지로 돌리는 것은 열정이 아니라 메모리 누수다.

## 2. 물리적 방화벽: GPU와 GIC-400의 단절
인터럽트 벡터 테이블(vector.S)을 구현하고 C++ 핸들러를 연결했음에도 CPU는 철저한 침묵(Deadlock)을 유지했다. GIC(Generic Interrupt Controller) 레지스터를 직접 타격하며 하드웨어 상태를 엑스레이 스캔해 본 결과, 칩셋 내부에 치명적인 라우팅 단절이 존재했다.

라즈베리 파이는 전원 인가 시 ARM CPU가 아닌 GPU(VideoCore VI)가 먼저 깨어나 하드웨어를 초기화한다. 이때 구형 OS와의 호환성을 위해 최신형 GIC-400 물리 회로를 강제로 끊어버리고 레거시 컨트롤러로 신호를 우회시킨다는 팩트를 확인했다.

* [해결책]
SD 카드의 config.txt에 단 한 줄의 물리적 스위치를 조작하여 GPU의 레거시 라우팅을 파괴했다.

```plain
enable_gic=1
```
## 3. 라우팅 블랙홀: TrustZone과 하이퍼바이저(EL2)의 기만극
GIC 회로가 연결되고 UART 하드웨어가 비명을 지르는 것([U], [G] Pending)을 확인했음에도 인터럽트는 여전히 C++ 핸들러로 넘어오지 않았다. CPU의 레지스터를 직접 스캔하여 알아낸 원인은 경악스러웠다.

라즈베리 파이 부트로더는 커널을 일반적인 정통 OS 모드인 **EL1(Kernel)**이 아니라, 가상 머신을 관리하는 EL2(Hypervisor) 권한으로 던져버린다. ARM 아키텍처의 규격상 EL2는 물리적 하드웨어 인터럽트를 자신이 받지 않고, 자신의 밑에서 돌아갈 가상 머신(EL1)으로 던져버리도록(Routing) 설계되어 있다.
즉, GIC가 코어 0의 문을 두드렸지만, 코어 0는 "나는 하이퍼바이저다. 이 인터럽트는 내 밑의 OS한테 줘라"라며 패킷을 허공에 버리고(Drop) 있었던 것이다.

## 4. 코어 로직: EL1으로의 '의도적 추락 (Fake Exception Return)'
임시방편으로 HCR_EL2 레지스터를 조작해 하이퍼바이저가 신호를 가로채게 만들 수도 있었지만, 이는 OSTEP 커널 이론에 위배되는 기형적인 아키텍처다. 리눅스 커널의 부트스트랩과 동일하게, 부팅 직후 코어 스스로 권한을 강등(Downgrade)시키는 정공법을 택했다.

ARM 아키텍처에는 권한을 낮추는 단순한 명령어가 없다. 따라서 **'가짜 인터럽트 복귀(Fake Return)'**라는 무자비한 해킹 기법을 boot.S에 하드코딩했다.

```plain
/* EL2에서 EL1(정통 커널 모드)으로의 의도적 추락 */

/* 1. 하이퍼바이저 설정: 하위 OS는 64비트(AArch64)로 동작한다 */
ldr     x0, =(1 << 31)
msr     hcr_el2, x0

/* 2. 가짜 복귀 상태 조작 (SPSR_EL2) */
/* "나는 원래 EL1에서 돌고 있었고, 모든 인터럽트가 마스킹된 상태였다"고 세뇌 */
mov     x0, #0x3C5
msr     spsr_el2, x0

/* 3. 가짜 복귀 주소 조작 (ELR_EL2) */
/* "내가 돌아갈 EL1 코드는 el1_entry 라벨이다" */
adr     x0, el1_entry
msr     elr_el2, x0

/* 4. 자유 낙하 (Drop to EL1) */
eret                

el1_entry:
/* 여기서부터는 완벽한 정통 OS 커널 모드(EL1)의 세계. C++ kernel_main() 진입 */
```
## 5. 최종 릴리즈: 진정한 비동기 이벤트 루프 완성
아키텍처를 EL1으로 리팩토링한 후, 기존의 쓰레기 같은 EL2 핫픽스들을 모조리 걷어냈다.

이제 메인 코어는 while(1) 안에서 wfi(Wait For Interrupt) 기계어를 만나 CPU 클럭을 끄고 완벽한 휴면(Sleep) 상태에 진입한다. 외부에서 키보드 타격이 발생하는 정확히 그 찰나의 순간에만 하드웨어 인터럽트를 통해 깨어나 데이터를 반사(Echo)하고 다시 수면에 빠진다. CPU 점유율 0%의 궁극적인 쿨링과 진정한 의미의 Asynchronous Event-Driven Architecture를 맨바닥에서 증명해 냈다.
